"""Configuración del admin para el módulo de autenticación.

El modelo `User` ya viene registrado por `django.contrib.auth`, por lo que
no es necesario registrar nada aquí. Se deja documentado a propósito.
"""
